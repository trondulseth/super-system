# Journal — 2026-08-21

## Phase 6: Release verification and archive

Completed release verification for `polish-library-quality` and archived both active OpenSpec changes.

### Fresh-install smoke tests

Packed `@super-system/react@0.1.0-beta.1` tarball and verified in clean projects:

| Framework | Result | Notes |
|-----------|--------|-------|
| Vite + React TS | Pass | Production build succeeds with Button, KpiCard, Sparkline, ThemeProvider |
| Next.js 16 App Router | Pass (after fix) | Initial build failed: bundled `react-dom` triggered "dynamic require" during SSR prerender |

**Fix applied:** Externalize `react-dom` in `packages/react` tsup build and declare it as a peer dependency. Bundle size dropped from ~97 KB to ~72 KB.

Smoke imports used: `@super-system/react`, `@super-system/react/styles.css`, `ThemeProvider defaultMode="light"`, `KpiCardChart` (not `KpiCardContent`).

### Living specs synced

Merged deltas from `polish-library-quality` into main specs:

- `react-components` — quality hardening, dashboard components (charts, KPI, layout, page shell), framework-compatible package output
- `accessibility` — modal labelling, pagination ellipsis, toast live region, empty-modal focus
- `theme-studio` — expanded token controls, config-only clarity, demo CSS separation
- `theme-system` — semantic overlay tokens

Validated with `openspec validate --specs --strict` — 7/7 passed.

### Archives

- `openspec/changes/archive/2026-08-21-polish-library-quality/`
- `openspec/changes/archive/2026-08-21-expand-react-component-library/`

### Not done (deferred to library-quality-pass-2)

- npm publish `0.1.0-beta.2`

Completed release verification for `library-quality-pass-2` and archived the change. All six review dimensions verified at 10/10.

### Test and build gate

- **107 automated tests** pass (`pnpm check` green: typecheck, vitest, package builds, studio demo build)

### Fresh-install smoke tests

Packed `@super-system/react@0.1.0-beta.1` tarball and verified in clean projects:

| Framework | Result | Notes |
|-----------|--------|-------|
| Vite + React TS | Pass | Production build with `KpiCardTrend trend="up"` (success token) and `DialogClose` default accessible name |
| Next.js App Router | Pass | Production build with `KpiCardTrend trend="up"` and `DrawerClose` default accessible name |

Smoke imports used: `@super-system/react`, `@super-system/react/styles.css`, `ThemeProvider defaultMode="light"`, KPI card composable parts, dialog/drawer with default close controls.

### Living specs synced

Merged deltas from `library-quality-pass-2` into main specs:

- `accessibility` — modal explicit naming, ref-counted background inert, toast live-region deduplication, disabled tooltip pattern, chart dataTable
- `react-components` — chart tones, success KPI trends, default close labels, scroll-safe overlays, disabled-tab skip, pagination unavailable state, vertical divider separator, bar chart stable keys, toast shadow tokens
- `theme-studio` — success colors, spacing unit, radius.full, hex sync, dual-theme contrast, save validation, preview-scoped motion, full preview coverage
- `theme-system` — semantic success tokens, distinct secondary/muted surfaces, focus token distinctiveness

### Score checklist (all pass)

| Dimension | Status |
|-----------|--------|
| Code architecture 10/10 | Scroll-safe overlays, DRY close, stable keys, ref-count inert |
| Accessibility 10/10 | 107 tests including a11y suite; default close labels, dataTable, inert background |
| Visual/token consistency 10/10 | No hardcoded colors; all tones/shadows tokenized |
| Default theme prettiness 10/10 | Distinct surfaces; success/focus tokens; mono visible |
| Studio controllers 10/10 | Full field coverage + validation + dual-theme contrast |
| Studio preview coverage 10/10 | Export checklist complete |

### Archive

- `openspec/changes/archive/2026-08-21-library-quality-pass-2/`

### Not done (user request only)

- npm publish `0.1.0-beta.2` — completed in release section below

---

## Release: 0.1.0-beta.2

Merged PR #14 (`library-quality-pass-2`) to `main` and published coordinated beta packages.

### Published packages

- `@super-system/tokens@0.1.0-beta.2`
- `@super-system/react@0.1.0-beta.2`
- `@super-system/cli@0.1.0-beta.2`

Install: `npm install @super-system/react@beta` (or `@super-system/cli@beta`, `@super-system/tokens@beta`).

See `MIGRATION.md` for breaking/behavior changes from beta.1.
